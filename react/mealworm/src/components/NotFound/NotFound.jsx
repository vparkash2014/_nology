import FeedbackPanel from './../FeedbackPanel'

const NotFound = () => {
    return (
        <FeedbackPanel title={'Page Not Found'}
        description={'The page you have requested does not exist.'}
        />
    )
}

export default NotFound;